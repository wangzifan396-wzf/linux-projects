# 复制本文件为 ~/.config/health-check.env 并填入真实值，即可激活 health-check 告警
# health-check.service 通过 EnvironmentFile 读取本文件（不存在则静默只写日志）
#
# 二选一：Telegram（推荐，手机直接收消息）或 通用 Webhook

# ---- Telegram 通知 ----
TG_BOT_TOKEN=xxxxxxxx:yyyyyyyy
TG_CHAT_ID=123456789
# 可选：国内访问 api.telegram.org 不稳时，经本机 SOCKS5 代理发出
# TG_PROXY=socks5://127.0.0.1:1080

# ---- 通用 Webhook 通知（如 Slack / 企业微信 / 自建接收端）----
# NOTIFY_WEBHOOK=https://hooks.example.com/xxxxx
